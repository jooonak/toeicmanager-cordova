{
	"$schema": "http://json-schema.org/draft-03/schema#",
	"id": "Build-WP8",
	"type": "object",
	"additionalProperties": true,
	"properties": {
		"DisplayName": {
			"type": "string",
			"required": true,
			"description": "A friendly name that can be displayed to users. The length of the DisplayName string should be between 1 and 256 characters.",
			"errorMessage": "The length of the DisplayName string should be between 1 and 256 characters.",
			"pattern": "^.{1,256}$"
		}
	}
}
